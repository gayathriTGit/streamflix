apiVersion: v1
kind: Service
metadata:
  name: streamflix-app
  labels:
    app: streamflix-app
spec:
  type: NodePort
  ports:
  - port: 80
    targetPort: 80
    nodePort: 30007
  selector:
    app: streamflix-app
